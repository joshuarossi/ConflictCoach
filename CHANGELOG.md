# Changelog

## Unreleased

### Added — WOR-23: Project Scaffolding

- Bootstrapped Vite + React 18 + TypeScript project with strict mode enabled
- Configured Tailwind CSS v3 with PostCSS and autoprefixer
- Installed shadcn/ui with Button component (`@/components/ui/button`)
- Set up React Router v6 with placeholder route at `/`
- Initialized Convex directory for backend development
- Added ESLint (flat config) and Prettier configurations
- Created `.env.example` documenting all required environment variables
- Added Vitest (unit) and Playwright (e2e) test configurations
- Configured `@/` path alias mapping to `src/`
